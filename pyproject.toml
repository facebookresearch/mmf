[tool.isort]
skip_glob = ["*/build/*"]
known_third_party = ["PIL", "coco_caption_eval", "cv2", "demjson", "git", "h5py", "lib", "lmdb", "maskrcnn_benchmark", "numpy", "omegaconf", "pycocoevalcap", "recommonmark", "requests", "setuptools", "sklearn", "sphinx_rtd_theme", "torch", "torchtext", "torchvision", "tqdm", "transformers", "yaml"]
multi_line_output = 3
line_length = 88
include_trailing_comma = true
use_parentheses = true
force_grid_wrap = 0

[tool.black]
line-length = 88
exclude = '''
/(
    \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | _build
  | buck-out
  | build
  | dist
)/
'''
